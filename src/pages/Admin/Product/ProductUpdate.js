import React from 'react';

const ProductUpdate = ({ product }) => {
    // Ajoutez ici la logique pour mettre à jour le produit dans la base de données
    return (
        <div>
            <p>Produit mis à jour avec succès !</p>
        </div>
    );
};

export default ProductUpdate;
